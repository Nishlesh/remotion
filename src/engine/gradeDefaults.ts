import type {GradeSpec} from './types';

/** Before It Was Famous — Magnates photoreal, warm film, grain. */
export const CHANNEL_GRADE: GradeSpec = {
  contrast: 1.08,
  saturation: 0.9,
  brightness: 0.97,
  cool: 0,
  warm: 0.16,
  grain: 0.28,
  vignette: 0.42,
};

export const DEFAULT_KEN_BURNS = {
  startScale: 1.0,
  endScale: 1.06,
  startX: 0,
  endX: 8,
  startY: 0,
  endY: 6,
};
